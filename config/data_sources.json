{
  "_comment": "Data source registry. Each entry defines a connection only — no static labels. All Prometheus filter labels (job, ooa, oop) are declared per job in jobs.json.",

  "production": {
    "type": "prometheus_via_grafana_proxy",
    "url": "https://grafana.mms.walmart.net/api/datasources/proxy/uid/production",
    "auth": null,
    "verify_ssl": false,
    "timeout_seconds": 30
  }
}
